import { IosButton } from './Buttons';
import { MacButton } from './Buttons';

export default function Hero() {
    return (
        <header className="tw-bg-clipboard-grayish-blue tw-min-h-96">
            
            <IosButton />
            <MacButton />
        </header>
    )
}