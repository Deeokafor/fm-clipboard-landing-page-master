import { IosButton } from './Buttons';
import { MacButton } from './Buttons';

export default function CallToAction() {
    return (
        <section>
            <IosButton />
            <MacButton />   
        </section>
    );
}